<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    >


    <ImageView
        android:id="@+id/artistListRecyclerArtistImageView"
        android:layout_width="175dp"
        android:layout_height="175dp"
        android:padding="5dp"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_launcher_background"

        >

    </ImageView>

    <TextView
        android:id="@+id/artistListRecyclerArtistsNameText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Selam Dünya"
        android:textSize="20sp"
        android:gravity="center"
        android:layout_alignStart="@id/artistListRecyclerArtistImageView"
        android:layout_alignBottom="@+id/artistListRecyclerArtistImageView"
        android:layout_marginBottom="25dp"
        android:layout_marginLeft="25dp"
        />


</RelativeLayout>